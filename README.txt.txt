0. Unzip.
1. To compile simply type: mvn test
2. To run on windows, simply type run (run.bat is the file on windows).
mvn exec:java -Dexec.mainClass="org.example.roman.Roman" 
Copy the command inside on the shell and execute on Unix.
